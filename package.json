{
  "name": "json-to.env",
  "version": "0.0.1",
  "description": "Converts json to .env file",
  "main": "main.js",
  "scripts": {
    "dev": "ts-node-dev --transpile-only --quiet --clear --respawn ./src/main.ts --require dotenv/config",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yuri Lima",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "node-cache": "^5.1.2",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
